# Membership

Not yet implemented.

Member fields: member ID, type, start/expiration date, status, payment
status. Member portal (`/member`) covers membership status, card, renewal,
events, formations, documents, certificates, profile — access restricted to
the member's own record via RLS.
