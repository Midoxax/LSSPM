# Integrations

Not yet implemented.

Planned: Supabase (DB/Auth/Storage), a payment provider (CMI Morocco,
behind an interface — see `finance.md`), email/WhatsApp/SMS for
notifications, and eventually Google Workspace (Drive, Calendar, Meet,
Gmail) with an org folder structure mirroring departments. Formal accounting
(e.g. Odoo) stays a separate system from operational finance, integrated
later rather than replaced.

GitHub: repo `Midoxax/LSSPM`, plain git remote. No GitHub App/MCP connected
in this session. Vercel: connected (team `upsy-final`), used for hosting
only — no production deploy has been made.
